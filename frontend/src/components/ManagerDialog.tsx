import { useState } from "react";
import { api } from "../lib/api";
export function ManagerDialog({ onClose, onSaved }: { onClose: () => void; onSaved: () => void }) { const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [capacity, setCapacity] = useState("10"); return <div className="modal-backdrop"><form className="modal" onSubmit={async (event) => { event.preventDefault(); await api.addManager({ name, email, dailyCapacity: Number(capacity) }); onSaved(); onClose(); }}><div className="modal-header"><h2>Новый менеджер</h2><button type="button" className="button button-ghost" onClick={onClose}>×</button></div><label>Имя<input required value={name} onChange={(event) => setName(event.target.value)} /></label><label>Email<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></label><label>Дневная вместимость<input required type="number" min="0" value={capacity} onChange={(event) => setCapacity(event.target.value)} /></label><button className="button button-primary">Добавить</button></form></div>; }
