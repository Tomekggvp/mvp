export function BoardToolbar({ query, setQuery, region, setRegion, product, setProduct, plannedFor, setPlannedFor, onOptimize, onAddManager, busy }: { query: string; setQuery: (value: string) => void; region: string; setRegion: (value: string) => void; product: string; setProduct: (value: string) => void; plannedFor: string; setPlannedFor: (value: string) => void; onOptimize: () => void; onAddManager: () => void; busy: boolean }) {
  return <div className="toolbar"><label className="date-filter">Переговоры на <input type="date" value={plannedFor} onChange={(event) => setPlannedFor(event.target.value)} /></label><input className="search-input" placeholder="Поиск заявки" value={query} onChange={(event) => setQuery(event.target.value)} /><select value={region} onChange={(event) => setRegion(event.target.value)}><option value="all">Все регионы</option>{["Витебск", "Могилёв", "Гомель", "Гродно", "Брест"].map((value) => <option key={value}>{value}</option>)}</select><select value={product} onChange={(event) => setProduct(event.target.value)}><option value="all">Все продукты</option>{["Кресла", "Столы", "Шкафы"].map((value) => <option key={value}>{value}</option>)}</select><span className="toolbar-spacer" /><button className="button button-secondary" onClick={onAddManager}>+ менеджер</button><button className="button button-primary" onClick={onOptimize} disabled={busy}>{busy ? "Считаем…" : "Рассчитать распределение"}</button></div>;
}
