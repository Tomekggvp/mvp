export interface Manager { id: string; name: string; email: string; active: boolean; dailyCapacity: number; createdAt: string; load?: number; }
export interface Lead { id: string; region: string; product: string; source: string; status: string; potentialAmount: number; createdAt: string; plannedFor?: string; }
export interface Assignment { leadId: string; managerId: string | null; expectedProbability: number; expectedGm: number; recommendedManagerId: string | null; recommendedManagerName?: string | null; recommendedManagerCapacity?: number; currentManagerName?: string | null; currentLoad?: number; source: string; assignedAt: string; recommendationSnapshot?: { probability: number; expectedSaleAmount: number; expectedGrossMarginOnSale: number; expectedGm: number; explanation: string; calculatedAt: string } | null; }
export interface ManagerScore { leadId: string; managerId: string; managerName: string; dailyCapacity: number; currentLoad: number; probability: number; expectedSaleAmount: number; expectedGrossMarginOnSale: number; averageDiscount: number; expectedGm: number; recentConversion: number; historicalConversion: number; observations: number; observations60: number; observations180: number; recentObservations: number; sales: number; smoothingWeight: number; priorConversion: number; averageCheck: number; formula: string; explanation: string; }
export interface BoardSnapshot { managers: Manager[]; leads: Lead[]; assignments: Assignment[]; kpis: { incoming: number; assigned: number; unassigned: number; expectedTotalGm: number }; lastRun?: unknown; }
export interface User { id: string; email: string; role: "employee"; }
